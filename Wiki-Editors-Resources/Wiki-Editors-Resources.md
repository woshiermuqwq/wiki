In this page 您将 find a somewhat complete summary of what the current syntax to document each page is. If 某事 不是 documented, ask about what the correct syntax 应为.

In each page 您将 find further descriptions as comments 在...内 page markdown 自身. You are invited to "edit" the page to see them.

## General Rules
- There should 总是 be two newlines (the character generated by pressing enter) between each "Section" (a Section being defined by the `## title`)
- - When using hyperlinks, one should 总是 be aware that the `/wiki/` path is the starting one, so relative paths可以madedoing 某事 like the following:。
`[`[ExampleMechanic](Wiki-Editors-Resources/ExampleMechanic)`
So, if a resource is on the wiki 自身, please do use this method.

## Embedded Data
There are bits of embedded data in the wiki in the form of html comments. These bits of data 用于 make datasets 对于 wiki and 它是 important that 它们是 not edited 没有 proper care

### 属性 Data
Those are added to the 默认 值 field of any 属性.
There are two 类型 of data can be embedded, and they可以used一起 and put in any order. So long as 它们是 *there* 某处 它是 fine。

This 也 works for universal 属性 即 not inside any specific 技能

`| type      | types, t  | A list of entity types to match           |<!--type:EntityType--><!--list--> |`

#### 类型
若used，this将willmake it so that the 属性 值 can 仅 be from a specific enum (like the Material or BarStyle ones, to say a few)。

For that purpose [一系列 valid 类型可以foundhere。](https://github.com/Lxlp38/MythicScribe/blob/master/generated/enumList/enumList.md) and can be put in any casing

#### 列表
若这是 used，it将willdeclare that the 属性 值 can be a comma separated 列表 of elements。
若used with `type`，each element将willneed to be 从 same enum。


### 触发器 Data
触发器 can have any one of those comments in any point in the page (preferably 仅 之前 the `## Description`)

- `MobTrigger` for 触发器 即 meant to be used by 生物 仅
- `PlayerTrigger` for 触发器 that all 玩家 can activate
- `ItemTrigger` for 触发器 具体 meant to be used from 物品
-- `ArchetypeTrigger` for 触发器 即 meant to be used in conjunction

`<!--type:PlayerTrigger-->`

The absence of any specified 类型 will make the 触发器 be considered "Universal" and can, as such, be used in multiple places. If in doubt, leave it like that.

### 目标选择器 Universal 属性 Data
D决定是否一系列 universal 属性 for 目标选择器 is 也 实体 ones or 位置 ones。

Realistically speaking, 您将 永不 need to touch this.

This is set by putting a comment in the 属性 header of the 属性 表

`| Attribute <!-- ETA --> | Aliases   | Description                                           | Default |`

The 值 可以是 ETA for 实体 or LTA for 位置

## 示例 Pages
- [技能 Wiki Page](Wiki-Editors-Resources/ExampleMechanic)
- [目标选择器 Wiki Page](Wiki-Editors-Resources/ExampleMechanic "Uses the same syntax as the 技能 one")
- [条件 Wiki Page](Wiki-Editors-Resources/ExampleMechanic "Uses the same syntax as the 技能 one")

## [Test Page](Wiki-Editors-Resources/Test-Page)

## Quick Copy-Pastes
```
## Attributes
| Attribute | Aliases   | Description                                                          | Default |
|-----------|-----------|----------------------------------------------------------------------|---------|
```
```
## Aliases
- [x] alias1
- [x] alias2
```
```
> **This is a [Paper-Only] mechanic!**

> **This is a [Premium-Only] mechanic!**

**<[Premium-Only]>**
```
```
<!-- LINKS -->
[metaskill]: /Skills/Metaskills
[mechanic]: /Skills/Mechanics
[targeter]: /Skills/Targeters
[trigger]: /Skills/Triggers
[condition]: /Skills/Conditions
[origin]: /Skills/Targeters/Origin
[audience]: /Skills/Audience

[Paper-Only]: https://papermc.io/downloads/all
[Premium-Only]: Premium-Features

[material]: https://hub.spigotmc.org/javadocs/bukkit/org/bukkit/Material.html

[Implemented Placeholders]: /Skills/Placeholders#variable-placeholders
```